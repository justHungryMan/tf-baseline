defaults:
  - downstream: bit


base:
  mode: train_eval
  target: imagenet
  env: 
    mode: tpu
    gcp_project: tpu0
    tpu_name: bit-s
    tpu_zone: europe-west4-a
    mixed_precision: True

  seed: '0xC0FFEE'
  pretrain: gs://jun_tpu0/bit-s/upstream
  save_dir: gs://jun_tpu0/bit-s/downstream/imagenet
  resume: False
